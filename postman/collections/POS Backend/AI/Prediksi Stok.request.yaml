$kind: http-request
url: "{{base_url}}/ai/predict-stock"
method: POST
headers:
  Accept: application/json
body:
  type: json
  content: "{\r

    \    \"query\": \"produk mana yang stoknya mau habis?\"\r

    }\r\n"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
