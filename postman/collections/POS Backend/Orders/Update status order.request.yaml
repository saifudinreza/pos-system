$kind: http-request
url: "{{base_url}}/orders/1/status"
method: PUT
headers:
  Accept: application/json
body:
  type: json
  content: "{\r

    \    \"status\": \"cancelled\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 6000
