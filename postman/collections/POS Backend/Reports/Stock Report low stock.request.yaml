$kind: http-request
url: "{{base_url}}/reports/stock?low_stock=true"
method: GET
headers:
  Accept: application/json
queryParams:
  low_stock: "true"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
