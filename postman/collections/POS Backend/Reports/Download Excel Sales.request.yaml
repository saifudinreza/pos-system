$kind: http-request
url: "{{base_url}}/reports/sales/download?format=excel"
method: GET
headers:
  Accept: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
queryParams:
  format: excel
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 6000
