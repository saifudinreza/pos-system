$kind: http-request
url: "{{base_url}}/tags"
method: POST
body:
  type: json
  content: "{ \r

    \    \"name\": \"Laravel\" \r

    }\r\n"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
